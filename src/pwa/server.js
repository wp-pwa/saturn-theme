import components from './components';
import Store from '../shared/stores';
import flow from '../shared/flows/server';
import { version } from '../../package.json';

export default components;
export { Store, version, flow };
